# PID_Beam_Controller
A PID control system implemented across a real system that controls the location of a cart on a tiltable beam via a linked motor. Motor is controlled via a pico running micropython.
